import { createTheme } from "@mui/material";

export default createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#ff4141",
    },
    secondary: {
      main: "#6c63ff",
    },
  },
  typography: {
    fontFamily: "Noto Naskh Arabic, serif",
  },
});
